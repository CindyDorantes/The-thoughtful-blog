<%= link_to user_path(user.id) do %> 
  <div class="user_box">
    <div class="user_pic">
      <%= image_tag user.photo %>
    </div>
    <div class="user_data">
      <h2><%= user.name %></h2>
      <span>Number of posts: <%= user.posts_counter %></span>
    </div>
  </div>
<% end %>